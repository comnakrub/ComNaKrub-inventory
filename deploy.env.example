# Deploy configuration — copy this file to deploy.env and fill in your values
# deploy.env is ignored by git (do not commit it)

REMOTE_HOST=192.168.99.105
REMOTE_PORT=22
REMOTE_USER=na
REMOTE_DIR=/share/Container/comnakrub
# หา path ของ docker บน QNAP ด้วย: ssh na@<host> 'find /usr /opt /share -name docker -type f 2>/dev/null'
DOCKER_BIN=/share/CACHEDEV1_DATA/.qpkg/container-station/usr/bin/.libs/docker
APP_PORT=8766
